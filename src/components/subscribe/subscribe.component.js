import template from './subscribe.html';
import controller from './subscribe.controler';

let subscribeComponent = {
  template,
  controller,
  controllerAs: 'subscribeCtrl'
};

export default subscribeComponent;